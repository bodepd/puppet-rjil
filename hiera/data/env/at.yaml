db_host: "%{lookup_array_first_element('services::db::eth0')}"
rjil::db::bind_address: "%{ipaddress_eth0}"

keystone::admin_bind_host: "%{ipaddress_eth0}"
keystone::public_bind_host: "%{ipaddress_eth0}"

glance::bind_host: "%{ipaddress_eth0}"
#glance::registry::bind_host: "%{ipaddress_eth0}"

keystone_public_address: "%{lookup_array_first_element('services::controller_load_balancer::eth0')}"
keystone_private_address: "%{lookup_array_first_element('services::controller_load_balancer::eth0')}"
glance_public_address: "%{lookup_array_first_element('services::controller_load_balancer::eth0')}"
glance_internal_address: "%{lookup_array_first_element('services::controller_load_balancer::eth0')}"

rjil::haproxy::openstack::keystone_ips: "%{lookup_array('services::keystone::eth0')}"
rjil::haproxy::openstack::keystone_internal_ips: "%{lookup_array('services::keystone::eth0')}"
rjil::haproxy::openstack::glance_ips: "%{lookup_array('services::glance::eth0')}"
# local address for keystone on controllers
# should be eth0 for real deployments, eth1 for vagrant
# openstack_extras::auth_file::controller_node: "%{ipaddress_eth0}"
openstack_extras::auth_file::controller_node: "%{ipaddress_eth0}"

glance::registry::auth_host: "%{hiera('keystone_public_address')}"
glance::api::auth_host:      "%{hiera('keystone_public_address')}"

rjil::keystone::public_port: 5000
rjil::keystone::admin_port: 35357

# ceph config
#
# currently, this ceph configuration just deploys a single ceph node until
# we understand how we are going to model the requirements for the ceph
# cluster
#
# currently, we are just putting everything onto one network for testing
rjil::ceph::storage_cluster_if: eth0
rjil::ceph::public_if: eth0

# autogenerate disks just to get functional storage nodes going
# until we better understand requirements for external disks
rjil::ceph::osd::autogenerate: true
rjil::ceph::osd::autodisk_size: 10
rjil::ceph::osd::osd_journal_size: 2

rjil::ceph::mon_leader: "%{lookup_array_first_element('services::stmonleader::eth1')}"
rjil::ceph::mon_array: "%{lookup_array('services::stmon::eth1')}"
